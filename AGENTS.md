A personal collection of Claude Code skills. Each skill is its own folder. The test suite for each skill lives in the `tests/` directory. Each skill gets its own test folder.

See the `README.md` for a list of the skills. See each skill's `README.md` for a detailed description.
