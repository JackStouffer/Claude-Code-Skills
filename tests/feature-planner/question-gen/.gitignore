# Only the reusable harness is tracked. Run outputs (subagent question JSON,
# generated prompts, judge scores) and the write-up are local scratch. The
# conclusion of a run goes in ../../../feature-planner/README.md.
runs*/
RESULTS.md
